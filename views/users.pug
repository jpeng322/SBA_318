html
  head
    style
      include ../style.css
    title= title
  body
    h1= header
    div.container
      each item, i in items  
        div.users-container
          h3= "Full Name: " + item.fname + " " + item.lname 
          p= "Username: " + item.username
          p= "Email: " + item.email 